{
  "name": "prologue-payout-processor",
  "version": "1.0.0",
  "description": "AWS Lambda function to process athlete payouts",
  "main": "process-payouts.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "stripe": "^14.5.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.5.0",
    "typescript": "^5.1.6"
  }
} 